Implement Fastlane + GitHub Actions for 2ogra: Create /fastlane(Fastfile,Appfile,metadata/*,gen_release_notes), .github/workflows/{ci,deploy-android,deploy-ios}.yml, CI_README.md. Lanes: a_beta internal, a_rel production, i_beta TF, i_rel AS, meta_sync, shots_opt; supply/pilot/deliver; upload Crashlytics dSYM+mapping. iOS auth: ASC API key or match. GHA: ci checkout+cache+setup F+JDK; tests. deploy-* tag/release+dispatch: tests+build FLAVOR (flutter build appbundle/ipa --flavor $FLAVOR -t lib/main_$FLAVOR.dart); sign+verify; fastlane; upload artifacts; prod gated via env. gen_release_notes on merge-to-main: commit titles since last tag + git shortlog; sanitize @users only; write en+ar-EG to fastlane/metadata/{android/<lang>/changelogs/<ver>.txt,ios/<lang>/whats_new.txt} incl 'Thanks @a,@b — vX.Y.Z'. Secrets: ANDROID_SERVICE_ACCOUNT_JSON, KEYSTORE_BASE64, KEYSTORE_PASSWORD, KEY_ALIAS, KEY_PASSWORD, GOOGLE_PLAY_TRACK, APP_STORE_CONNECT_API_KEY_BASE64, APP_STORE_CONNECT_API_KEY_ID, APP_STORE_CONNECT_API_ISSUER_ID, IOS_EXPORT_METHOD, FIREBASE_TOKEN?, MATCH_PASSWORD?, SLACK_WEBHOOK?. CI_README: secrets+env rules+rollback (Play promote prev/unpublish; App Store reject/submit prev).